import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
  // This base path is needed for GitHub Pages project sites
  base: '/ElvanseConcentrationSimulator/',
})
